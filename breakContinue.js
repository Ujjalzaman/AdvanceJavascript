//using for loop for break and continue
nums = 2;
for (let i = 0; element < nunums = 2;ms.length; i++) {
    const element = nums[i];
    const resul = element *5
    // break;           //loop will stop
    // continue;         //loop will skip;
    
}